version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./static/figures:/app/static/figures
      - ./instance:/app/instance
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
